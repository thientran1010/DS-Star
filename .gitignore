# Ignore everything by default
*

# Keep directories visible so negations can work (safe even if not needed)
!*/

# Allow only these files at repo root
!.gitignore
!.python-version
!README.md
!config.yaml
!dsstar.py
!prompt.yaml
!provider.py
!pyproject.toml
!uv.lock
